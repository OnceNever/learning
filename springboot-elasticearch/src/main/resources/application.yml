server:
  port: 8080
#数据库配置
mybatis:
  type-aliases-package: cn.seeyourface.elasticearch.entity
  mapper-locations: classpath:mapper/*.xml
spring:
  datasource:
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/elasearch?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
